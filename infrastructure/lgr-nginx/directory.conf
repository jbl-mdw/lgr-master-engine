server {
  listen 80;
  server_name directory.southsuburbsbest.com;
  root /var/www/directory;
  index directory.html index.html;

  location /health.txt { return 200 "ok\n"; add_header Content-Type text/plain; }
}
